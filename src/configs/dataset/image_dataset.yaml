train:
  _target_: src.dataset.ImageDataset
  data_dir: "data/VCDF_faces"
  part: "train"
  val_size: 0.2
  shuffle_index: True
  instance_transforms: ${transforms.instance_transforms}
  augmentations: ${transforms.augmentations}

val:
  _target_: src.dataset.ImageDataset
  data_dir: "data/VCDF_faces"
  part: "val"
  val_size: 0.2
  instance_transforms: ${transforms.instance_transforms}

test:
  _target_: src.dataset.ImageDataset
  data_dir: "data/hwei_part1_faces"
  part: "val"
  val_size: 1.0
  instance_transforms: ${transforms.instance_transforms}
